%h2 Sign in
= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => 'bp'}) do |f|
  = f.input :email
  = f.input :password
  = f.check_box :remember_me
  = f.label :remember_me

  = f.submit "Sign in"

= render :partial => "devise/shared/links"